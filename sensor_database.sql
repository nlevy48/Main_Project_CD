CREATE Table sensor_data (
    SensorID INT PRIMARY KEY,
    SensorName VARCHAR(50)
    SensotValue FLOAT
    Timestamp DATETIME DEFAULT CURRENT_TIMESTAMP

INSERT INTO sensor_data (SensorID, SensorName, SensorValue)
)